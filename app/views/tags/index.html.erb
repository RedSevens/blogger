<h1>All tags</h1>

<ul id="tags">
	<% @tags.each do |tag| %>
		<li><%= link_to tag.name, tag_path(tag) %></li>
	<% end %>
</ul>
<br/>
<%= link_to "<< Go Back to Articles List", articles_path %><br />